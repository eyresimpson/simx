// use std::collections::HashMap;

// 发起Get请求
// pub async fn get(url: &str) -> Result<HashMap<String, String>, reqwest::Error> {
//     Ok(reqwest::get(url).await?.json::<HashMap<String, String>>().await?)
// }
//
// // 发起Post请求
// pub async fn post(url: &str) -> Result<HashMap<String, String>, reqwest::Error> {
//     Ok(reqwest::get(url).await?.json::<HashMap<String, String>>().await?)
// }
